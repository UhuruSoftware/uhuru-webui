---
applications:
  .:
    name: wordpress
    framework:
      name: php
      info:
        mem: 128M
        description: PHP Application
        exec: !!null 
    url: ${name}.${target-base}
    mem: '512'
    instances: 1
    services:
      wordpressDB:
        type: mysql
      wordpressFS:
        type: mysql
