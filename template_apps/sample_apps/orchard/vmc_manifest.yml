---
applications:
  .:
    name: wptestman
    framework:
      name: php
      info:
        mem: 128M
        description: PHP Application
        exec: !!null 
    url: ${name}.${target-base}
    mem: '512'
    instances: 1
    services:
      wptestmanDB:
        type: mysql
      wptestmanFS:
        type: uhurufs
