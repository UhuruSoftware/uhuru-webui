applications:
  .:
    framework:
      info:
        description: .Net Web Application
        mem: 128M
        exec:
      name: php
    mem: 128M
    url: ${name}.${target-base}
    instances: 1
    name: dotnetwithmongo-uhurumonitoring9vkz2
    services:
      type: rabbitmq
